{% extends "crud/base.html" %}
{% block content %}
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <title>文章列表</title>
    <!-- 引入 Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* 自訂樣式 */
        .search-page {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        .search-header {
            text-align: center;
            margin-bottom: 20px;
            width: 100%;
        }

        .search-bar {
            width: 300px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }

        .content-container {
            display: flex;
            width: 100%;
            max-width: 1200px;
        }

        .category-buttons {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-right: 20px;
        }

        .category-btn {
            width: 150px;
            margin-bottom: 10px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #007bff;
            color: white;
            cursor: pointer;
            text-align: left;
            transition: background-color 0.3s;
        }

        .category-btn:hover {
            background-color: #0056b3;
        }

        .category-btn.active {
            background-color: #0056b3;
        }

        .popular-articles {
            flex: 3;
            padding: 10px;
            border-left: 1px solid #ccc;
        }

        .article-card {
            margin-bottom: 20px;
        }

        .article-title {
            font-size: 1.5rem;
            font-weight: bold;
        }

        .article-meta {
            font-size: 0.9rem;
            color: #888;
        }

        .article-summary {
            font-size: 1rem;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="search-page">
        <!-- 上方區塊 -->
        <div class="search-header">
            <h1>文章搜尋</h1>
            <form method="GET" action="{{ url_for('search') }}" class="d-flex justify-content-center">
                <input type="text" name="query" placeholder="搜尋文章..." class="search-bar me-2" value="{{ request.args.get('query', '') }}">
                <button type="submit" class="btn btn-primary">搜尋</button>
            </form>
        </div>

        <!-- 下方區塊 -->
        <div class="content-container">
            <!-- 左側分類按鈕 -->
            <div class="category-buttons">
                <button class="category-btn {% if not selected_category %}active{% endif %}" onclick="window.location.href='{{ url_for('articles') }}'">全部</button>
                {% for category in categories %}
                    <button class="category-btn {% if selected_category == category.name %}active{% endif %}" onclick="window.location.href='{{ url_for('articles', category=category.name) }}'">{{ category.name }}</button>
                {% endfor %}
            </div>

            <!-- 中間熱門文章 -->
            <div class="popular-articles">
                <h2>熱門文章</h2>
                {% for article in articles.items %}
                    <div class="card article-card">
                        <div class="card-body">
                            <h5 class="card-title article-title">{{ article.title }}</h5>
                            <h6 class="card-subtitle mb-2 text-muted article-meta">
                                分類：{{ article.category.name }} | 瀏覽次數：{{ article.view_count }}
                            </h6>
                            <p class="card-text article-summary">{{ article.summary }}</p>
                            <a href="{{ url_for('view_article', article_id=article.id) }}" class="card-link">閱讀更多</a>
                        </div>
                    </div>
                {% endfor %}

                <!-- 分頁 -->
                <nav aria-label="Page navigation">
                    <ul class="pagination justify-content-center">
                        {% if articles.has_prev %}
                            <li class="page-item">
                                <a class="page-link" href="{{ url_for('articles', page=articles.prev_num, category=selected_category) }}" aria-label="上一頁">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                        {% else %}
                            <li class="page-item disabled">
                                <span class="page-link">&laquo;</span>
                            </li>
                        {% endif %}

                        {% for page_num in articles.iter_pages(left_edge=2, right_edge=2, left_current=2, right_current=2) %}
                            {% if page_num %}
                                {% if page_num == articles.page %}
                                    <li class="page-item active"><span class="page-link">{{ page_num }}</span></li>
                                {% else %}
                                    <li class="page-item"><a class="page-link" href="{{ url_for('articles', page=page_num, category=selected_category) }}">{{ page_num }}</a></li>
                                {% endif %}
                            {% else %}
                                <li class="page-item disabled"><span class="page-link">…</span></li>
                            {% endif %}
                        {% endfor %}

                        {% if articles.has_next %}
                            <li class="page-item">
                                <a class="page-link" href="{{ url_for('articles', page=articles.next_num, category=selected_category) }}" aria-label="下一頁">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        {% else %}
                            <li class="page-item disabled">
                                <span class="page-link">&raquo;</span>
                            </li>
                        {% endif %}
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <!-- 引入 Bootstrap JS (可選) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
{% endblock %}
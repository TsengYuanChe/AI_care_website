{% extends "crud/base.html" %}
{% block content %}
<div class="container mt-5">
    <h1>{{ article.title }}</h1>
    <h6 class="text-muted">
        分類：{{ article.category.name }} | 瀏覽次數：{{ article.view_count }} | 發布時間：{{ article.filtered_at.strftime('%Y-%m-%d %H:%M') }}
    </h6>
    <hr>
    <p>{{ article.content }}</p>
    <a href="{{ url_for('articles') }}" class="btn btn-secondary">返回文章列表</a>
</div>
{% endblock %}